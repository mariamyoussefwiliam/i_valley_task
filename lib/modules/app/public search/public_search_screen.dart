import 'package:flutter/material.dart';

class PublicSearchScreen extends StatelessWidget {
  const PublicSearchScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("PublicSearchScreen");
  }
}
