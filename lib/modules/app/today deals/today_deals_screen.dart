import 'package:flutter/material.dart';

class TodayDealsScreen extends StatelessWidget {
  const TodayDealsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("TodayDealsScreen");
  }
}
